# BM25查询清理与对照实施计划

用户已批准先修查询侧、保留Embedding和RRF的方案。本轮实现第一步，其余标题导航、权威地域元数据和子问题拆分按效果继续，不混入本次对照。

**目标：** 向量路输入问题原文，BM25路保守删除口语套话并补少量明确同义检索词。默认启用，支持`--raw-bm25`还原对照。

**边界：** 纯Python、无新依赖、无LLM重写或GPU；不删除“不/未/不得”、金额、条号、期限、法名、引号内原文。不做全局单字停用词删除。规则删除使用空格隔开，不能拼出新词；清理为空回用原文。仅“租房”补“住房租赁”，排除公租房、廉租房、出租房等复合词；这是检索扩展，不作为法律概念等价认定。保持过滤、召回100/候选50/RRF60/展示5不变。

**步骤与验证：**

- [x] 先写查询清理、否定/引号/复合词保护、两路输入分离及raw开关测试，观察RED。
- [x] 新增`legal_query/keywords.py`，接入CLI与simple_search；正文、ID、引用不变。纯BM25同样使用规则，输出实际bm25_query供本地用户检查。
- [x] 新增24题合成问题固定集与对照入口；每题仅调用一次Embedding，两组共用向量。分别留存BM25和RRF前5完整结果、问题向量缓存与清理后查询。缓存放Git忽略的artifacts，不写来源库。
- [x] 重建镜像，26项容器unit、Ruff、6源码mypy、Compose校验通过；真实运行前后Strong计数921117不变。
- [x] 检查24题前5文书/条号变化及北京原文，报告改善、退步及不变项。执行时不采用预设文书命中比例：同一主题有多种合法文书，单凭标题会误判。没有独立律师金标，不报告法律准确率。
- [x] 更新使用说明与项目现状，保留全部对照证据和未完成边界，不提交Git。
