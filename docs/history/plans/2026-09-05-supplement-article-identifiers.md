# 增补条号身份保留

真实只读样本 `F:\ai律师数据库\法律法规数据库\法律\中华人民共和国刑法_20201226.docx` 经当前 Parser 得到465条，但许多“第X条之一/之二”与“第X条”共用provision_no；直接阻塞唯一编号导入，分块首段也会残留“之一”。仅修复结构识别，不修改法律原文或推断效力。

- [ ] 合成测试 RED：基础条+之一/之二各自独立，全文/段落边界保留；中文、阿拉伯、全角数字，正文包含对增补条的引用不误分条。
- [ ] 统一 parser 条头与 chunk 引擎去条头的识别规则，完整保留“条之X”身份，避免两个不同正则再次漂移。保留旧基础条号契约与仅长条拆分/歧义回退。
- [ ] 准备/导入映射验证同一基础条的增补条无重复，长增补条子块去掉完整条号但父块逐字不变。
- [ ] 同一真实样本只读复核数量/重复编号/覆盖，不复制外部法律原件入仓库、不猜版本日期；必要时报告仍有的其它结构问题。
- [ ] 聚焦pytest、Ruff/mypy、独立审查及现状更新；不重建业务库或发布索引。

范围：infrastructure/documents/parsers.py、application/legal_chunk_structure.py、必要共享domain条号识别模块及对应unit。S3已完成代码，正在最终组合验收，本切片不改S3服务/适配器。
