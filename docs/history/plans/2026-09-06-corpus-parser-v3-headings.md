# corpus-docx-v3 明确条头修复

已批准范围：真实全集中8份可由原导出证明的条头漏识别。其余4份缺头不放宽条号序列门禁。

1. TDD 加入零/〇、数字与“条”间横向空白、段落内部软换行条头案例，以及行内引用和歧义反例。
2. 共享条头正则识别零及横向空白；身份字段去除条号内空白，权威正文保留原字符。Parser 仅在段内换行后的明确条头拆分；普通续文换行保持原样。紧凑内部条头可能是引用时拒绝，不猜测。
3. 根任务将入口默认 Parser 标识改为 corpus-docx-v3；修复已导入来源必须使用显式新 version_label，保留旧版本、证明和发布记录。新识别导致结构 SHA 及证明变化，不静默修改旧版本。
4. 聚焦 Parser/共享分块/质量测试与 Ruff、mypy；对12份既有导出只读重解析，8份序列通过、4份继续拒绝。

本次已观察新测试12 failed、1 passed，再修复。初步 Parser/无条号回归34 passed；只读重解析确认8份条序完整，4份仍在原缺口拒绝。未读取或改写外部来源，未执行入库或索引发布。
