# 单源静态恢复审核导入

关联 [真实全集发布](2026-09-06-full-real-corpus-release.md)。仅处理已有独立逐段核验的静态恢复来源；保留全部恢复限制，不修改 Parser 或来源。

显式审核证书路径与 SHA-256 成对输入，证书引用原独立核验文件及 SHA-256。严格验证完成、零问题、正文/页眉/条序一致、转换身份及本次来源读取哈希。默认缺省仍拒绝。Proof 的 JSON quality_flags 保存审核 SHA 与绑定当前来源/派生/转换/结构身份的摘要；发布 selection 必须显式携带同一 SHA，不能只凭非空字符串放行。无需迁移。

先合成测试 RED→GREEN，覆盖错误哈希、错误来源、未完成/有差异、伪造孤立flag及重放冲突；之后真实单源只读预检。真实入库由全集任务在独立复审后执行。

## 本次证据

- 审核绑定5项测试先RED；最终相关9组324 passed，Ruff、9个源文件mypy、diff检查通过。新增文件读取/身份不一致/未完成/段序差异/显式匹配digest反向测试。
- 真实单源仅预检：38条、2辅助段、blockers为空。原静态恢复限制flags全部保留，没有重新转换或修改来源。
- 审核证书 `artifacts/legal-corpus/reviews/static-recovery-reviewed-20260906.json` SHA-256 `f94f7c3e87782c98b02d2ea21da573506bde38ff9c831341a7e3bc4149413914`，引用既有独立核验快照；这是操作者显式选择的审核事实，不是密码学审批签名。
- 制定机关及地域按同一来源SHA精确读取 authority-evidence 与 authority-region-source-assignments-v2，分别为厦门市人民代表大会常务委员会、350200；未从标题猜测。
- 本切片未做真实写入、业务迁移或静态恢复索引发布，待独立复审后由全集任务继续。
