# 新解析版本的前导标题条号识别

本切片服务于已批准的真实全集入库/发布工作，依据 `.superpowers/sdd/title-reference-parser-design.md` 的只读证据。它不代表全集解析纠错或发布完成。Task 1/2 已完成；最终独立复审 `.superpowers/sdd/leading-title-task-1-final2-review.md` 两项 PASS。完整任务差异 `leading-title-final-v2.diff` SHA-256 `2601a52a2f34222898ff69eee0b6d276fd594fe0d80fb2b75625831bd5bc12b6`，5 文件原始字节身份单独保存。

最终验证：合成聚焦 66 passed；全体后端 unit/api/contract 2625 passed、1 平台符号链接 skipped、1 既有 Starlette warning，Ruff/mypy 216 源文件通过。真实 10/10 来源经完整读取、源哈希、span 原段位置及显式全文模式覆盖检查通过，输出 `artifacts/legal-corpus/full-release/leading-title-v4-readonly-check-20260907-final2.json`；导入 0、索引写入 0。首轮 7/10 及两次失败复审均保留，后续的“本法”检查仅精确排除“基本法”的重叠命中，未绕过规范名内部的禁止正文信号。

## 全局约束

- 原件只读，保留旧数据库事实、Chunk ID 和报告，不调用模型、不写数据库、不切索引别名。现有 CPU 向量进程继续运行，禁止第二模型和 GPU 计算。
- 只在显式 `corpus-docx-v4` 解析身份下启用新规则；默认构造器、旧 v3 和其它现有标签维持当前行为。v4 是尚未用于真实入库的开发身份，所有必要修正完成后固定实现及依赖快照，才能开始长批次。
- 不放松连续条号门禁，不通过自动全文回退掩盖重复或歧义。无编号正文仍须显式 `content_mode=non_article_document`。
- 规则不依赖具体真实文件 SHA、完整标题、机关名称或条号。保留来源段落文本、换行、ordinal；新增标题 span 用原段落索引/ordinal/段内字符范围追溯，不能假设 ordinal 唯一。
- 保留所有用户未提交改动，不提交、不清理、不移动工作树。合成测试遵循行为 RED → GREEN；真实材料不放入测试 Fixture。

## Task 1：实现与接入

- [x] 在独立小模块实现有界的前导标题 span 检测；在 `LegalStructureParser` 提供显式 profile，并由 `corpus_publish._prepare` 对精确 v4 标签选择新 profile。入口级测试证明选择实际生效。
- [x] 从首个非空段开始，到独立括号日期沿革段组为止；沿革必须包含年月日和通过/批准/公布等沿革动作，可以跨最多 3 个相邻非空段，必须闭合且不含正文条头；找不到明确边界则维持旧行为。标题和沿革之间只允许额外 1 段严格的 `法释〔年份〕序号号` 文号（也可使用同类配对方括号），文号不计为标题。整个探测限定最多 12 个非空段及总计 4096 字符，越界不猜。该补充来自 251bf6de 的两段沿革和 442522c1 的法释号，见 `.superpowers/sdd/leading-title-real-targets.json`。
- [x] 前导区首段首个非空行不能是条头；拼接视图须含“关于”、成对规范名边界 `《…》` 或 `<…>`，最终结尾为解释、决定或批复。只有受限尾部语法完整匹配的条号行才获得标题分类；支持顿号/和连接的多条号、款引用、的解释、使用/适用问题的解释、处罚权限规定的决定、的批复等已见结构。句号、分号、冒号、引号及本法/本条例/本解释/为了/应当等正文提示阻止该分类。不得用任意文本加最终后缀的宽松规则吞掉真实条文。
- [x] 标题 span 必须在沿革段前结束，并在现有内部换行歧义检查前生效。原文原样保留；新分类可通过 ParsedInstrument 的附加默认空元数据表达，不建立本任务用不到的 citation graph 或元数据标题匹配机制。
- [x] 合成回归涵盖独立段/内部换行/尖括号/多条号/款/批复标题、两段闭合沿革、可选法释号；沿革之后真实第一、二条和短条；无沿革、错配/未闭合括号、多个文号、无规范边界、首段是条头、正文标点/规范词、超过界限的反例；ordinal 重复也不能豁免错误段；旧 profile 行为不变。
- [x] 独立实现子代理完成后生成本任务前后快照 diff，独立复审；处理重要问题后复审通过。

## Task 2：只读真实复核与交接

- [x] 根代理对设计记录中的 9 个目标及补充的刑法第344条批复只读复核，核对原文 span 和正文条号；区分“标题误识别修复”“仍需显式全文模式”和其它剩余问题。
- [x] 最终后端聚焦及全体 unit/api/contract、Ruff、mypy；更新现状、验证边界与后续新版本冻结入口。不得将标题切片完成写成全集发布完成。
