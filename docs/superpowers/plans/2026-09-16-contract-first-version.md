# 合同审阅第一版实施计划

## 2026-09-21 严格两次多模态调用（当前流程）

用户已明确两次调用都包含看图读取。成功的 window_v2 路径严格为：第一次全部页图＋原生全文生成总结 → 总结驱动一次第二版RRF＋rerank Top7 → 第二次同样全部页图＋原生全文＋相关法律片段一次输出风险JSON。使用 LlamaIndex Workflow 事件骨架及受控MCP；旧模型选文、独立视觉读取、两条分页、模型纠正和第三次内容复核不在本路径调用。

- [x] TDD验证一次摘要、一次总结查询、Top7来源全部保留、无模型选文；来源、哈希、地域和检索回执保留。
- [x] 两次都发送全部页图；原生坐标留在程序，不把大量几何JSON重复送模型；不发送max_tokens，超过30页等资源边界仍显式拒绝。
- [x] 第二次最多100条风险的一次JSON（不要求凑数）；程序给法律片段编号并绑定引用原文，未知/缺失法律引用的候选独立登记为withheld，不偷偷修造引用或额外调用。
- [x] 原文/锚点/范围/租户授权/保存门禁保留。无坐标的视觉引用标为待核对；结果明确是未经独立语义复核的AI风险草稿，不作为正式法律结论，引用匹配不代表支持论证。
- [x] 后端321项、前端23项、修改源码mypy、前端类型和构建通过；本次修改文件Ruff及git diff --check通过。
- [x] 真实23页两次调用均23张图；1次RRF/rerank、7窗口、1次片段读取；MySQL草稿保存及只读核验通过。
- [ ] 法律质量通过：当前7条草稿仍有条款误读、计算和依据适用错误，2条精确定位、5条待核对；不得用流程通过代替质量验收。后续在两次调用边界内改进生成质量与引用定位，使用既有捕获优先离线分析。

记录：`.superpowers/local-contract/qwen-two-pass-20260921-005849/`。此增量只验收本地风险草稿流程；不改变正式意见的人工批准或公网多租户生产要求。

## 2026-09-20 有效审阅修复（旧分批方案由上方两次调用覆盖）

目标为真实合同产出由原文支持的具体风险与可执行改写，保留全文覆盖、每轮最多两条、证据与租户门禁。沿用既定ReAct/MCP架构；原生坐标由后端精确回填，模型不猜坐标；材料效力未知时只生成交易协商草稿，不能冒充已核验法律结论。

- [ ] 工作流TDD：精简模型引用协议（块ID＋原文摘录），程序从本次权威块恢复锚点；保留完整合同文字，按页组织当前批次，全量覆盖不可漏。
- [ ] 修正TDD：拒绝重复JSON键；格式重试提供已知字段的上一轮候选及具体错误，不保留隐藏推理/任意字段；约束具体风险后果与可直接使用的改写，拒绝将排版拼接误判合同错误。
- [ ] 研究输入：核对本轮候选地域与法律长文负担；只读既有法律原件与索引，针对相关章节组织审阅证据，保留权威全文身份/来源/哈希和未知效力。用合成测试验证无捏造、无跨文档引用。
- [ ] 聚焦回归、独立复核及真实23页验证：保存本地完整输入输出、具体意见、模型usage、最终门禁和网页持久化结果；不能以Schema成功替代实质意见或全流程完成。

用户已确认第一版业务链路；采用 TDD 与独立子任务评审。保留当前脏工作树，不提交无关变更。沿用已有源码模块，避免建立另一套账号/权限或重复目录。

## 约束

- PDF无文字要求时默认中立审阅；版面/识别保留可靠锚点，无法完整识别显式失败或待复核。
- 中国大陆法律范围；RAG使用已有Docker Milvus/BM25/向量/RRF；Top7按文档去重，LLM最多选择3份；未知效力/来源不能冒充已核验依据。
- 法律全文从只读派生文件恢复，固定身份/哈希/解析版本；分页有界，不能重复拼接父子块或接受任意本地路径。
- 文件、结果、调用、缓存必须绑定可信租户/用户/运行；用户文件与检索文本均为不可信输入。
- 输出仅阶段和白名单摘要；法律意见经引用/原文/内容门禁，保存成功后才显示批注；不输出隐藏推理。
- 保持既有业务功能，不改F盘原件/派生事实或Milvus集合，不启动重复向量任务；不打印密钥。

## 任务

- [x] 核对段落/文档派生格式和Milvus身份映射，形成有界只读全文访问方案。
- [x] 法律工具：真实RAG文档候选与分页全文读取，接受登记ID；合成测试拒绝路径越界/正文不完整/身份错配；真实Docker只读验证。
- [x] Workflow：合同摘要、检索、Top7筛选、最多3份全文读取、审查校验与批注阶段；新增真实SDK链路测试和无结果/故障/超限分支。
- [x] PDF与Web接入：受控上传、版面/识别锚点、预览及气泡、SSE进度；适配现有租户认证和存储授权，不用开发绕过伪装公网完成。
- [x] 复核与交付：关键边界独立审查；按变更执行pytest/Ruff/mypy、前端类型/测试/构建；记录实际外部调用、未验证服务和运行方式，更新现有说明及源码导航。

阅读与实施按任务边界进行。新增生产依赖同步锁文件，测试先观察预期失败再实现。网络/模型缺失时明确报告，不以合成响应代替真实运行。

## 验证边界与接续

代码与真实后端联合验证见[现状](../../project-status.md)。网页接入复用原账号/租户体系，尚未启动完整环境做浏览器登录上传联合验收；用户PDF留待此后。模型输出的覆盖/原文/锚点结构错误可最多一次修正，始终重新经过全部门禁；不放宽法律效力与语义支持要求。Word、独立语义reranker、法律元数据、进程崩溃恢复与生产容量不在本次完成证明之内。
# 2026-09-17 交互修复增量

用户已明确要求模型、拖放修复和 ChatGPT 风格。保持既有未提交改动，直接在当前工作区修复，不提交 Git，不增添新的独立业务模块。

- [x] 复现普通对话仅装配 DeepSeek、PDF 无拖放事件；新增 Qwen SSE 与装配失败测试，修复后聚焦验证。
- [x] 在现有 ContractReviewView 统一对话/附件入口；ChatView 复用该工作区。先写 DOM 拖放、登录/租户、取消和普通消息路由测试，再实现。
- [x] AppShell 改为侧栏，PDF 保持现有坐标组件；验证窄屏与附件错误反馈，保留其他页面入口。
- [x] 后端聚焦 pytest/Ruff/mypy，前端 DOM/API 测试、类型与构建；重启本机 API，验证真实 Qwen 对话和合同鉴权上传，更新状态。
# 2026-09-17 Qwen 多模态替换实施增量

目标：按用户确认的“Qwen看图理解＋原生坐标定位”替换合同主流程中的规则表格/OCR，沿用现有MCP、鉴权、证据与保存接口。此增量在当前工作区执行，保留其它改动，不提交Git。

- [x] 模型图片边界：domain/model_gateway.py 增加有界图片值对象，DashScope适配器序列化图片；先测图片真实传递、秘密不进repr、非法/过大输入拒绝、计量和文字接口兼容。
- [x] 文档视觉读取：新增 contract_review/vision.py 实现页图阅读和原生引用定位；复用pdf.py受控渲染/容量，不调用find_tables或RapidOCR。测试覆盖漏页、跨租户拒绝、分批页序、模型转录不得替换原文、扫描待复核；原文/坐标/覆盖使用既有门禁回归。复杂合并表格的视觉准确率尚未验收。
- [x] Runtime接入：装配视觉文档服务及统一调用计量，视觉转录传给理解/审阅/独立校验；保持受控MCP/门禁/失败清理；阶段文字改为Qwen阅读，错误显示具体中文原因。
- [x] 接入验证：后端聚焦222通过，Ruff/mypy249通过；前端71通过及类型/构建。真实合成合同在浏览器完成红色标记和气泡；客户23页完成上传、预览与模型读取，但因document_incomplete未出批注，计量与状态读回。详见现状。
- [ ] 复杂合同验收：完善看图覆盖判断与可诊断的页级核对；客户合同全流程仍未通过，不用合成成功替代。扫描PDF精确批注仍缺少可靠定位方案。

## 2026-09-17 摘要驱动检索

用户明确要求先由Qwen总结合同，本地MCP拿内容总结进行RAG。当前只传摘要阶段生成的检索词；本增量补充内容总结，不将合同全文作为查询。

- [x] 在现有research测试和runtime测试中先断言RAG实际输入包含合同类型、内容总结和检索重点，排除合成全文标记；无效摘要不得触发工具调用，观察预期失败。
- [x] 修改application/contract_review/research.py：摘要最长1200字符、单个检索重点最长200字符；由ContractBrief构建每次不超过2000字符的查询，保留最多7次检索、合并Top7和最多3份法律全文。摘要Prompt强调概括而非转录，未知信息不编造。
- [x] 运行合同相关pytest、Ruff及mypy，更新现状和运行说明；确认本地无活动审阅后重启对应API。Mock请求验证不冒充真实模型召回质量验收。不改解析器、批注门禁或既定法律全文阅读范围。

## 内容核对失败修复

- [x] 定位布尔覆盖标记直接阻断、无页级诊断，以及不同字体的填充值/单位拆行造成的原生辅助文字顺序问题；保留原引用块和锚点。
- [x] TDD覆盖纯空白/整行重排、金额/单位/否定词/重复行、扫描无坐标、矛盾模型标记；原生字形辅助排序不得增删字符，不新增OCR或版面服务。
- [x] 固定页码/原因码报告接入失败计量清单、授权读取和SSE中文提示；原文线索和模型观察不进入报告。隔离MySQL实测保存、读回和同租户非所有者/跨租户拒绝。
- [x] 完成最终聚焦检查、真实模型核对及本地API重启：后端195、隔离MySQL1、前端相关21通过，Ruff/mypy通过。真实23页不再阻断9/10/20/23页，但第18页疑似缺字仍阻断；单独复核遇HTTP429，详见现状。
- [x] 额度恢复后定位第18页：模型将绘图填写横线误报为缺字。补充明确Prompt与最多一次/4页的定向复核；真实缺字、异常页码与剩余失败页仍阻断。TDD后端199通过，Ruff/mypy249通过；单页真实调用通过，2929 tokens。
- [x] 完整23页通过内容核对，定位后续草稿120秒超时；按用户要求切换qwen3.6-flash，移除摘要/审阅不需要的定位元数据，保留全文、锚点与权威门禁；TDD相关200通过，Ruff/mypy通过，新模型小调用成功。
- [x] 复现Flash合法JSON在摘要跨字段一致性检查失败；发现Schema把业务必填的contract_type/queries标为可省略。统一必填声明、拆分六类一致性原因、补充安全日志及最多一次摘要重新生成；TDD聚焦210通过，Ruff/mypy249通过。
- [x] 原生全文摘要分支真实验证：23页690块→405字摘要→7个检索重点→MCP/RAG Top7→2份全文，24,587 tokens；仅只读研究，不代表视觉覆盖或批注验收。
- [x] 第7页核对阻断的处理方案已被用户最新决定替代：内容核对仅警告继续，见下节。

## 内容核对改为警告（用户最新要求）

- [x] runtime、受控MCP准备及引用门禁显式允许不完整材料，保留报告与真实完整性标记。
- [x] 视觉补充块不生成坐标；转录意见用null锚点显示位置待核对，原生锚点和法律校验保留。
- [x] 报告接入完成清单、GET/SSE和网页提醒；缺字/乱码/不可读/不确定与无原生字形的完整流程回归通过。
- [x] 后端215、隔离MySQL1、前端72通过，Ruff/mypy249、前端typecheck/build通过。
- [x] 补充HTTP/SSE警告至result/done链路断言，后端API12及前端API10通过。
- [ ] 客户23页合同真实网页最终批注验收：本次摘要、RAG及全文读取通过，草稿阶段model_unavailable；9次成功调用共189,603 tokens，另一次错误无usage。异常具体原因尚需安全诊断。

## 草稿长输出修复（2026-09-17）

- [x] 真实诊断确认供应商finish_reason=length，24,576输出上限截断；按用户要求提高到65,536、单次等待300秒后仍复现截断，不能仅增加预算。
- [x] 用户进一步要求取消本地token上限：合同请求省略max_tokens/max_completion_tokens，Provider支持None并保持原有显式配置兼容。官方默认值仍等于模型最大输出65,536；不宣称提高了服务硬上限。
- [x] 长合同按96块分批生成批注；每批保留全文和法律上下文，仅输出本批覆盖与意见，全部合并后统一门禁和一次保存。保留原自定义ReAct循环、原文锚点和法律证据检查。
- [x] 模型输出Schema显式给出；截断使用独立错误，最多一次重新生成，每次调用仍计入预算。合同运行模型步数增至40，支持视觉读取、研究及分批审查。
- [x] TDD覆盖分批完整覆盖、漏块拒绝、截断修正与重复截断不保存；官方上限参考[Qwen3.6 Flash](https://help.aliyun.com/zh/model-studio/qwen3-6-flash)。
- [x] 使用官方JSON Object模式；ReAct四步循环保留，工具动作改用action/action_input JSON，支持旧格式解析以保持兼容。真实模型仍漏抄/重复覆盖ID，改为绑定当前运行的批次确认，后端恢复该批权威ID；未知批次、跨批意见和旧格式漏块仍拒绝。修正反馈只含安全字段/原因/范围计数。
- [x] JSON模式真实原生全文摘要分支通过：355字摘要、7个重点、Top7和3份全文，摘要3.769秒。该分支不能替代网页或视觉覆盖验收。
- [x] 实测定位重复检索重点引发摘要重生成；改为字段类型/长度验证后按序去重，保留所有不同重点、摘要与空值/分类检查。TDD覆盖两次模型调用内进入检索且不重复查询。
- [ ] 完成真实23页客户合同最终批注验收，再更新现状与验证结果。

## 每轮两条问题与运行缓存（2026-09-20 用户指定）

- [x] 合同Provider改为官方SSE流式接收、真实usage、终止/截断检查与取消关闭；原失败提示词真实流式重放已取得首段和终止证据，不代表重复问题已消除。完整记录定位同一条款重复250/188次，严格两条约束仍在生成后校验。
- [x] 本地服务装载流式及视觉恢复增量：2026-09-20已重启独立API 8001，网页代理登录/授权租户读取均200；临时记录API 8002已停止。

- [x] 所有合同统一按最多两条新意见分页，轮次回执、明确批次完成标记和私有候选缓存接入原ReAct循环；完整材料继续提供。
- [x] 测试先复现旧代码无法2＋2＋1连续输出，再实现缓存回传、去重、拒绝过期轮次/跨批/超量/无进展，失败与预算耗尽不发布局部结果。
- [x] 原文/锚点修正也按两条返回，只允许定位字段变化；最终覆盖/内容/法律门禁及一次保存保留。运行模型次数改100，输出token参数仍省略。
- [x] 本轮263项相关测试及修改文件Ruff/mypy通过，真实Qwen合成协议按2＋2＋1＋0结束、5条意见通过原文/锚点检查并进入内存测试存储；4次调用9,751 tokens。服务已装载。合成协议验证不代替23页客户合同最终网页验收。
# 2026-09-20 用户批准的页面读取修复切片

目标：原生正文保留、视觉核对补充、局部读取失败带警告继续；完整法律和租户门禁不变。

- [x] Provider与聚焦测试：仅识别真实JSON生成中止为稳定错误，不将鉴权/配置/任意400当作可重试JSON错误，流式/非流式均保留计量边界。
- [x] vision与聚焦测试：native页补充模式、扫描页单页、批失败拆单页、JSON异常一次非流式、最终失败警告继续、扫描失败有界分区、取消与权限/预算不吞掉。
- [x] runtime装配：两个同供应商网关共享计量与调用预算，非流式仅由视觉恢复分支显式选择；新增失败原因在API/网页说明。
- [x] 聚焦pytest、Ruff/mypy、独立复核；第7页与23页真实流程，不隐藏之后出现的其他阻断。314项后端及22项前端通过，类型/构建通过；23页读取、摘要/RAG/全文通过，审阅第二条意见缺字段导致model_output_invalid，最终批注仍未验收。
