# 混合版本向量补算设计

按用户持续推进全集向量化的授权执行；保持现有51项运行文件不可变，不改模型、GPU参数、来源或业务事实。现有两批覆盖当前发布集合15,882版本，另95个质量通过版本需要补充验证/计算，17个集合外阻断版本暂不能进入补算。实际缓存缺口见mixed-vector-coverage-20260907T064210Z.json。

采用独立混合选择协议与补算入口。直接修改旧v3 runner会破坏活动进程冻结；把v4标成v3会伪造身份；因此新增文件并复用原有Provider、缓存及温控组件。新选择明确绑定完整mixed release清单、完整质量报告文件SHA、固定模型1792维/L2、逐版本叶数量/摘要及顺序。父报告整体有阻断时，只允许明确选中的无阻断版本；不修改父报告passed，也不据此批准发布。

选择Reader无模型，严格读取大小/类型/重复键/来源报告绑定；逐版本从完整provenance恢复原source parser与chunk parser。执行时在同一只读一致事务重新加载该版本质量与叶块：以父quality的单版本facts_sha256重建旧schema单版本质量摘要，与当前质量服务实际结果相等，另校验完整summary、dataset/source parser及叶摘要。不得仅比较条数。断点恢复须同选择SHA、模型、维度及cache目录、干净排空/进程退出/运行SHA无变化/无事件和缓存错误；已完成前缀实际回读缓存。

补算与旧任务共用单模型锁，FP32、window2/outer8、25% allocator，68°C冷却/62°C恢复/80°C硬停。每源最多持有当前文档叶块与8向量，缓存命中复用；每源全部回读验证后递增游标。新运行记录完整实际本地import闭包和锁文件；不装载第二模型，不启动OpenSearch。失败不自动重试，正常任务只允许排空退出后串行接续。实际GPU补算验收必须等待旧任务完成；纯测试与预检不能代替CUDA结果。

验收包括清单/父证明/身份/叶变更拒绝、单版本事实变化、恢复错选择与旧进程存活拒绝、缓存前缀验证、温控/取消排空、真实95版本只读预检、后续真实GPU补算日志及最终完整发布门禁。本切片不降低35个质量问题、12份未入库或全集检索/发布的验收要求。
