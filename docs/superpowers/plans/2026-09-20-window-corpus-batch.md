# 全量滑动窗口语料批次

用户已批准三样本后扩大至批量，完成后再用 RAGAS 评测。沿用1000字符、200字符重叠，embedding_text严格为法名＋完整窗口。该用户决定允许此离线新批次跨条/跨章，不修改已有法律条文索引和发布规则。

实施采用 writing-plans/TDD；本任务在现有用户分支上增加一个批处理入口和一个测试文件，不提交或覆盖既有脏改动。

- [x] `scripts/bulk_window_corpus.py`：Docling重新解析16,006份来源（旧doc复用已验证转换，docm只读不开宏）；以官方纯文本序列化保留表格/列表，去空段和furniture；保存全文、原件/转换/解析哈希、质量标记及窗口位置。
- [x] `scripts/tests/test_bulk_window_corpus.py`：先验证预期失败；覆盖窗口还原、字段输入、无空块、DOCX表格/页脚、身份及缓存参数绑定、损坏缓存拒绝、乱序向量及非有限值拒绝。新增来源清单防替换、元数据恢复、退避停止和精确转换输入选择测试，共11项通过；独立代码审查3项P2已修复并复核。
- [x] 固定代码/依赖/源清单快照，输出在原件根之外 `F:/律师Agent派生数据/滑动窗口第二版-20260920`；旧清单只用于来源身份与追溯，不能把旧chunk拼成新全文。
- [x] 每个文档/向量批次独立落盘且核验哈希，续跑只接受同版本同输入；不累计全量向量。以最多4个进程有界运行，定期回收worker；请求每批最多10条，120秒超时，429/5xx有界重试，鉴权错误停止新请求；失败不冒充完成。
- [x] 代表样本5份/297窗口已真实向量化；五个独立补批修复特殊格式3份、输入绑定33份、表格空白1份、失效界面关系14份，共337窗口已独立核验。输入绑定按冻结input_sha256选择唯一既有转换，表格空白使用v3保留非空白字符序列；UI修复仅删除准确匹配的缺失界面关系。全集已完成，保留资源/进度/真实用量记录，不中途改运行快照，不重算已核验缓存。
- [x] 全量处理后独立核对文档数、失败/质量清单、全文切片/向量身份及数量，报告实际位置。索引切换和RAGAS实际测评在数据准备完成后进行，不拿成功向量化冒充检索质量通过。

旧正文预估90,495,035字符、117,206窗口；其中一份旧paragraphs.jsonl解码失败，不使用其损坏内容。全量预估不是完成计数。先前Docling样本的跨条统计不等于坏块；批量质量以文本完整性、追溯、索引与后续检索评测分别核验。

验证入口：独立Docling环境运行 `python -m unittest discover -s scripts/tests -p test_bulk_window_corpus.py -v`，再Ruff检查新增脚本；批次入口提供prepare/run/status/verify，完成记录与恢复协议分别检验。暂不改变生产依赖和服务进程。

最终验收：`release-verification-20260920-193203/report.json`通过16,006份/117,976窗口的文件、输入和向量一致性核验，0错误。6,369份质量警告保留，其中7份/55段字面覆盖待核对；不冒充内容无损或RAGAS通过。主/补批已返回usage为60,953,730 tokens，3次网络请求计费结果未知，恢复连通性另29 tokens。
